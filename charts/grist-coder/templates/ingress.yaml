{{/*
  Ingress produit par le library-chart, comme pour les autres services du catalogue :
  hote, classe d'ingress et cert-manager viennent du contexte Onyxia, la protection
  par IP de security.allowlist, et il disparait quand le service est suspendu.
*/}}
{{- if .Values.ingress.enabled }}
{{- $_ := required "ingress.hostname est obligatoire : un Ingress sans hote capterait tout nom non attribue du controleur partage." .Values.ingress.hostname }}
{{- end }}
{{ include "library-chart.ingress" . }}
