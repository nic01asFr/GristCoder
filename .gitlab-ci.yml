# CI GitLab CEREMA — distribution GristCoder MCP pour Onyxia SSPCloud.
#
# Deux livrables, tous deux hebergees sur ce projet GitLab (public, id 3099) :
#   1. Image conteneur -> registre conteneur GitLab ($CI_REGISTRY_IMAGE).
#   2. Chart Helm       -> registre Helm GitLab (canal stable), pull anonyme.
#
# La CI reecrit image.repository du chart avec $CI_REGISTRY_IMAGE au packaging :
# le chart publie pointe donc TOUJOURS sur l'image reellement poussee, sans devinette.
#
# Declencheurs : branche par defaut (latest) et tags v* (version figee).

stages:
  - build-image
  - publish-chart

# ── 1. Build + push de l'image (kaniko : pas de privileged requis) ─────────────
build-image:
  stage: build-image
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [""]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG =~ /^v/
  script:
    - |
      DESTS="--destination ${CI_REGISTRY_IMAGE}:latest"
      if [ -n "$CI_COMMIT_TAG" ]; then
        DESTS="$DESTS --destination ${CI_REGISTRY_IMAGE}:${CI_COMMIT_TAG#v}"
      fi
    - mkdir -p /kaniko/.docker
    - |
      cat > /kaniko/.docker/config.json <<EOF
      {"auths":{"${CI_REGISTRY}":{"username":"${CI_REGISTRY_USER}","password":"${CI_REGISTRY_PASSWORD}"}}}
      EOF
    - /kaniko/executor
        --context "${CI_PROJECT_DIR}"
        --dockerfile "${CI_PROJECT_DIR}/Dockerfile"
        $DESTS

# ── 2. Package + push du chart Helm vers le registre Helm GitLab ───────────────
publish-chart:
  stage: publish-chart
  image: alpine/helm:3.15.4
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG =~ /^v/
  before_script:
    - apk add --no-cache curl
  script:
    # image.repository <- valeur autoritative du registre conteneur pousse en etape 1.
    - sed -i "s#^\(\s*repository:\).*#\1 ${CI_REGISTRY_IMAGE}#" charts/grist-coder/values.yaml
    - helm package charts/grist-coder --destination .
    - CHART_TGZ=$(ls grist-coder-*.tgz | head -1)
    - echo "Publication ${CHART_TGZ} -> registre Helm GitLab (canal stable)"
    - |
      curl --fail --request POST \
        --user "gitlab-ci-token:${CI_JOB_TOKEN}" \
        --form "chart=@${CHART_TGZ}" \
        "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/helm/api/stable/charts"
